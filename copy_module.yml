---
- name: copy files
  hosts: all 
  become: true
  become_user: root
  tasks:
    - name: copy files amonng different folder
      copy:
       src: /tmp/abc
       dest: /tmp
       owner: root
       group: root
       mode: 0755

